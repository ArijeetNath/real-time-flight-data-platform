# Optional: OpenSky OAuth2 client credentials (free account at opensky-network.org).
# Leave blank to use anonymous access (works, but tighter rate limits).
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=

# Seconds between ingest batches in loop mode.
INGEST_INTERVAL=300
